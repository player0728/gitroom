# 数据同步工具使用说明（增强版）

## 一、概述

`data_sync_tool_plus.py` 是一个统一的数据同步配置向导工具，集成了数据同步和 Git 仓库管理功能（更新为dameng_test.py）（更新为dameng_new.py）。

### 支持的功能模块

| 序号 | 功能模块 | 说明 |
|-----|---------|------|
| 1 | 数据同步 | SeaTunnel 配置向导，支持多种数据源 |
| 2 | Git 仓库同步 | 克隆、检查更新、拉取代码 |

### 数据同步支持的数据源类型

| 序号 | 数据源类型 | 目标类型 | 说明 |
|-----|-----------|---------|------|
| 1 | MySQL 数据库 | MySQL 数据库 | 支持 CDC 实时同步和批量同步，自动获取库/表/字段结构，支持字段映射，自动创建目标表 |
| 2 | HTTP API | MySQL 数据库 | 支持随机用户 API 和自定义 API，自动解析 JSON 结构 |
| 3 | 达梦数据库 | 达梦数据库 | 达梦数据库之间的数据同步，自动获取库/表/字段结构，支持字段映射，自动创建目标表 |
| 4 | 达梦 CDC 实时监控 | 达梦数据库 | 基于触发器的达梦到达梦实时同步，支持配置多个监控任务，支持查看状态、停止、删除 |
| 5 | 测试模式 | Console | 使用 FakeSource 生成测试数据 |

## 二、安装依赖

### MySQL 同步依赖
```bash
pip install mysql-connector-python
```

### 达梦数据库同步依赖
```bash
pip install pyodbc
```

### API 数据源依赖
```bash
pip install requests
```

### 完整安装
```bash
pip install mysql-connector-python pyodbc requests
```

### Git 支持
需要安装 Git 客户端：https://git-scm.com/downloads

## 三、使用方法

### 启动工具
```bash
python dameng_new.py
```

### 主菜单

启动后显示主菜单：
```
============================================================
  数据同步与Git仓库管理工具
============================================================

请选择功能模块：
  1) 数据同步（SeaTunnel 配置向导）
  2) Git 仓库同步
  3) 退出
请选择 [1-3] [默认：3]: 
```

## 四、数据同步功能详解

### 4.1 MySQL 数据库同步

#### 支持的作业模式
- **STREAMING**：实时同步（CDC 模式）
- **BATCH**：批量同步

#### 配置步骤
1. 输入作业名称
2. 选择作业模式
3. 输入并行度
4. 配置源 MySQL 数据库连接信息
5. 配置字段映射（可修改目标字段名、类型，删除或添加字段）
6. 配置目标 MySQL 数据库连接信息
7. 确认执行

#### 自动功能
- 自动检测可用数据库列表
- 自动检测数据库中的表列表
- 自动获取表结构并生成字段映射
- 自动创建不存在的目标表

### 4.2 HTTP API 同步

#### 支持的 API 类型
- **Random User Generator API**：预置的随机用户数据 API（https://randomuser.me/）
- **自定义 API**：用户自定义的 API 接口

#### 自定义 API 配置
- API URL：API 接口地址
- HTTP 方法：GET 或 POST
- JSON 内容字段：数据所在的 JSON 字段路径（支持嵌套，如 `data.items`）

#### 自动功能
- 自动测试 API 连接
- 自动解析 API 返回的 JSON 结构
- 自动提取字段名和类型

### 4.3 达梦数据库同步

#### 支持的功能
- ✅ 自动获取数据库列表
- ✅ 自动获取表列表
- ✅ 自动获取表字段信息（Schema）
- ✅ 字段映射配置（可修改目标字段名、删除不需要的字段）
- ✅ 自动创建不存在的目标表（根据字段映射配置）

#### 配置步骤
1. 配置源达梦数据库连接信息（主机、端口、用户名、密码）
2. 自动获取并显示可用的数据库列表，选择数据库
3. 自动获取并显示该数据库中的表列表，选择表
4. 自动获取并显示所选表的字段结构（Schema）
5. 字段映射配置（可以修改字段名、删除不需要的字段）
6. 配置目标达梦数据库连接信息
   - 如果输入的表不存在，会询问是否根据字段映射自动创建
7. 确认执行同步

#### 工作流程示例
```
【步骤 2/5】配置源达梦数据库
--- 配置源达梦数据库 ---
源达梦主机地址 [默认：127.0.0.1]:
源达梦端口 [默认：5236]:
源达梦用户名： SYSDBA
源达梦密码： ********

可用的数据库：
  1) CTISYS
  2) DMHR
  3) TEST
请选择数据库 [1-3] 或输入数据库名： 3
✓ 已选择数据库：TEST

数据库 'TEST' 中的表：
  1) SOURCE_TABLE
  2) USERS
请选择表 [1-2] 或输入表名： 1
✓ 已选择表：SOURCE_TABLE

表 'SOURCE_TABLE' 的字段结构：
============================================================
序号   字段名                    类型
============================================================
1      ID                       INT
2      NAME                     STRING
3      AGE                      INT
4      ADDRESS                  STRING
============================================================
共 4 个字段

【步骤 3/5】字段映射配置
是否修改字段映射？[y/N]:
  1) ID → ID (INT)
  2) NAME → NAME (STRING)
  3) AGE → AGE (INT)
  4) ADDRESS → ADDRESS (STRING)
确认以上字段映射？[Y/n]:
```

#### 自动创建目标表
如果目标表不存在，会自动创建：

```
数据库 'TEST' 中的表：
  1) SOURCE_TABLE
  2) USERS
请选择表 [1-2] 或输入表名： NEW_TABLE
✗ 表 'NEW_TABLE' 不存在于数据库中
是否根据字段映射创建此表？[Y/n]: y

目标表不存在，根据字段映射创建表结构...
执行建表SQL: CREATE TABLE "TEST"."NEW_TABLE" ("ID" INT NULL, "NAME" VARCHAR(255) NULL, "AGE" INT NULL);
✓ 目标表创建成功
```

#### 同步逻辑
- **数据相同**：跳过（不执行任何操作）
- **主键相同，数据不同**：更新
- **主键不存在**：插入

#### 数据类型支持
| 字段类型 | 达梦类型 |
|---------|---------|
| INT | INT |
| DATETIME | DATETIME |
| BOOLEAN | BOOLEAN |
| STRING | VARCHAR(255) |

## 五、达梦 CDC 实时监控功能详解

### 5.1 功能概述

达梦 CDC 实时监控功能基于数据库触发器实现，能够实时捕获源表的增删改操作，并同步到目标表。

#### 支持的特性

- ✅ 基于触发器的实时数据捕获
- ✅ 支持配置多个监控任务
- ✅ 自动检测表主键
- ✅ 支持查看监控任务状态
- ✅ 支持删除监控任务
- ✅ 支持停止监控任务
- ✅ 每次启动自动清理旧变更日志表
- ✅ 任务列表显示当前状态（运行中/已停止）

### 5.2 达梦 CDC 菜单

选择达梦数据库后，进入 CDC 菜单：
```
============================================================
  达梦 CDC 实时监控
============================================================

请选择操作：
  1) 配置 CDC 监控任务
  2) 启动 CDC 监控服务
  3) 停止 CDC 监控服务
  4) 查看监控状态
  5) 删除监控任务
  6) 返回主菜单
请选择 [1-6] [默认：6]: 
```

### 5.3 配置 CDC 监控任务

配置步骤：
1. 配置源达梦数据库（主机、端口、用户名、密码）
2. 自动检测并选择数据库，选择要监控的表
3. 自动检测表的主键
4. 配置目标达梦数据库（主机、端口、用户名、密码）
5. 选择目标表
6. 配置轮询间隔（秒）

#### 配置示例
```
【步骤 1/5】配置源达梦数据库
--- 配置源达梦数据库 ---
源达梦主机地址 [默认：127.0.0.1]:
源达梦端口 [默认：5236]:
源达梦用户名： SYSDBA
源达梦密码： ********

可用的数据库：
  1) CTISYS
  2) DMHR
  3) TEST
请选择数据库 [1-3] 或输入数据库名： 3
✓ 已选择数据库：TEST

数据库 'TEST' 中的表：
  1) USERS
  2) TARGET
请选择表 [1-2] 或输入表名： 1
✓ 已选择表：USERS

表 'USERS' 的字段结构：
============================================================
序号   字段名                    类型
============================================================
1      USER_ID                   INT
2      USERNAME                  STRING
3      PASSWORD                  STRING
4      AGE                       INT
5      PHONE                     STRING
============================================================
共 5 个字段

【步骤 2/5】选择要监控的表
✓ 已选择监控表：USERS

【步骤 3/5】检测主键字段
✓ 自动检测到主键字段：USER_ID

【步骤 4/5】配置目标达梦数据库
--- 配置目标达梦数据库 ---
目标达梦主机地址 [默认：127.0.0.1]:
目标达梦端口 [默认：5236]:
目标达梦用户名： SYSDBA
目标达梦密码： ********

可用的数据库：
  1) CTISYS
  2) DMHR
  3) TEST
请选择数据库 [1-3] 或输入数据库名： 3
✓ 已选择数据库：TEST

数据库 'TEST' 中的表：
  1) USERS
  2) TARGET
请选择表 [1-2] 或输入表名： 2
✓ 已选择表：TARGET

【步骤 5/5】配置监控选项
轮询间隔（秒）[默认：5]:

✓ CDC 配置已保存

✓ CDC 监控任务配置完成！
```

### 5.4 启动 CDC 监控服务

启动时会自动：
1. 删除旧变更日志表
2. 创建新变更日志表
3. 删除旧触发器（如果存在）
4. 创建新触发器（INSERT、UPDATE、DELETE）
5. 开始实时监控

启动示例：
```
【启动 CDC 监控服务】

已配置的 CDC 监控任务：

  1) TEST.USERS → TEST.TARGET [stopped]

请选择要启动的任务 [1-1]: 1

创建变更日志表...
ℹ 已删除旧的变更日志表
✓ 变更日志表创建成功

为表 'USERS' 创建 CDC 触发器...
✓ 表 'USERS' 的 CDC 触发器创建成功

✓ CDC 监控已启动，正在监控变更...

按 Ctrl+C 停止监控

检测到 3 个变更
  ✓ UPDATE: PK=1
  ✓ UPDATE: PK=2
  ✓ UPDATE: PK=3
```

### 5.5 查看监控状态

查看已配置的监控任务及其当前状态。

### 5.6 停止监控服务

停止正在运行的监控服务。

### 5.7 删除监控任务

删除不需要的监控任务配置。

### 5.8 工作原理

1. **触发器捕获**：在源表上创建 3 个触发器（INSERT、UPDATE、DELETE），捕获数据变更
2. **变更日志**：触发器将变更记录保存到变更日志表（DAMENG_CDC_CHANGE_LOG）
3. **轮询同步**：监控循环定期查询变更日志表，将未处理的变更同步到目标表
4. **标记处理**：同步完成后，标记变更记录为已处理

### 5.9 数据同步逻辑

| 操作类型 | 处理逻辑 |
|---------|---------|
| INSERT | 在目标表插入相同数据 |
| UPDATE | 根据主键更新目标表数据 |
| DELETE | 根据主键删除目标表数据 |

## 六、Git 仓库同步功能详解

### 6.1 克隆远程仓库

```
请选择操作：
  1) 克隆远程仓库
  2) 检查更新
  3) 拉取最新代码
  4) 查看仓库信息
  5) 返回主菜单
请选择 [1-5] [默认：5]: 1

【克隆远程仓库】
仓库地址 (HTTPS/SSH): https://github.com/username/repo.git
本地路径: ./myrepo
```

### 6.2 检查更新

```
【检查更新】
本地仓库路径: ./myrepo
当前分支：main
本地提交：abc1234
远程提交：def5678
发现新提交！

更新日志：
def5678 Update README.md
abc1234 Initial commit

是否立即拉取更新？[y/N]: y
```

### 6.3 拉取最新代码

```
【拉取最新代码】
本地仓库路径: ./myrepo
正在拉取最新代码到：./myrepo
代码拉取成功
```

### 6.4 查看仓库信息

```
【查看仓库信息】
本地仓库路径: ./myrepo

仓库信息：
远程地址：https://github.com/username/repo.git
当前分支：main
工作区干净
```

## 七、文件结构

```
d:\software\apache-seatunnel-2.3.5\
├── dameng_new.py            # 数据同步工具（主文件，含达梦 CDC 功能）
├── data_sync_tool_plus.py    # 增强版数据同步工具（保留）
├── data_sync_tool.py         # 原数据同步工具（保留）
├── git_sync_tool.py          # 原 Git 同步工具（保留）
├── seatunnel_mysql.py        # 原 MySQL 同步工具（保留）
├── dameng_sync_direct.py     # 原达梦同步工具（保留）
├── 数据同步工具使用说明_增强版.md    # 增强版说明文档
├── 数据同步工具使用说明.md           # 原说明文档（保留）
└── config/                   # 自动生成的配置文件目录
    ├── cdc_tasks.json        # CDC 监控任务配置
    └── *.conf               # SeaTunnel 配置文件
```

## 八、注意事项

1. **环境变量**：建议设置 `SEATUNNEL_HOME` 环境变量指向 SeaTunnel 安装目录
2. **达梦 ODBC 驱动**：使用达梦数据库同步功能需要提前安装 DM8 ODBC 驱动
3. **Git 安装**：使用 Git 功能需要提前安装 Git 客户端
4. **网络访问**：克隆远程仓库需要网络访问权限
5. **数据安全**：请妥善保管数据库密码和 Git 仓库凭证

## 九、更新日志

### v3.0（达梦 CDC 实时监控版）
- 新增达梦 CDC 实时监控功能
  - 基于触发器的实时数据捕获
  - 支持配置多个监控任务
  - 支持查看、停止、删除监控任务
  - 任务列表显示当前状态（运行中/已停止）
- 新增自动检测表主键功能
- 优化触发器创建逻辑（移除 AS 关键字，兼容达梦）
- 变更数据存储改为管道分隔格式（兼容达梦）
- 每次启动自动清理旧变更日志表
- 修复目标表错误（正确更新目标表而非源表）

### v2.1（达梦功能增强版）
- 新增达梦数据库自动获取 schema 功能
  - 自动获取数据库列表
  - 自动获取表列表
  - 自动获取表字段信息（Schema）
- 新增达梦字段映射配置功能
  - 可修改源字段和目标字段的映射关系
  - 可删除不需要的字段
- 新增达梦自动创建目标表功能
  - 根据字段映射配置自动创建目标表
  - 支持 INT、DATETIME、BOOLEAN、STRING 等数据类型
- 完善达梦数据同步逻辑
  - 先查询再决定操作（插入/更新/跳过）
  - 避免误报更新

### v2.0（增强版）
- 新增 Git 仓库同步功能
- 集成数据同步和 Git 管理到统一界面
- 优化达梦数据库同步逻辑
- 改进错误处理和用户提示

### v1.0（基础版）
- 支持 MySQL 数据库同步（CDC/BATCH）
- 支持 HTTP API 同步
- 支持达梦数据库同步
- 支持测试模式